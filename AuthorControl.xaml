<UserControl x:Class="Dns.AuthorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Dns"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="400">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Selected book:" Margin="5"/>
        <TextBox Grid.Row="1" Grid.Column="0" Name="txtSelectedBook" Margin="5" IsReadOnly="True"/>

        <Label Grid.Row="2" Grid.Column="0" Content="Author:" Margin="5"/>
        <TextBox Grid.Row="3" Grid.Column="0" Name="txtAuthor" Margin="5" IsReadOnly="True"/>

        <Label Grid.Row="2" Grid.Column="1" Content="Bio:" Margin="5"/>
        <TextBox Grid.Row="3" Grid.Column="1" Name="txtBio" Margin="5" IsReadOnly="True"/>

    </Grid>
</UserControl>
